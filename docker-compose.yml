version: "3"
services:
  swagger-ui:
    image: swaggerapi/swagger-ui
    volumes:
      - ./docs/openapi.json:/usr/share/nginx/html/openapi.json
    environment:
      API_URL: openapi.json
    ports:
      - "8080:8080"
