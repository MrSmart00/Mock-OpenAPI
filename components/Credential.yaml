type: object
required:
  - email
  - password
properties:
  email:
    type: string
    format: email
    example: mock@example.com
  password:
    type: string
    format: password
    example: password1234
